---
// ChapterHead · 章节头
export interface Props {
  tag: string;
  title: string;
  lede: string;
}

const { tag, title, lede } = Astro.props;
---

<div class="ch-head reveal">
  <div class="ch-tag">{tag}</div>
  <h2>{title}</h2>
  <p class="ch-lede">{lede}</p>
</div>
